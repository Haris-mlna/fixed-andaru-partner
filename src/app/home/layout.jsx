import Loading from "../loading";

export const metadata = {
	title: "Home | Business Partner",
	description: "Generated by create next app",
};

export default function HomeLayout({ children }) {
	return (
		<main>
			{children}
		</main>
	);
}
